# Виджет погоды EWW для Гомеля, Беларусь

## 📋 Обзор

Этот виджет отображает текущую погоду в Гомеле с обновлением каждые 30 минут. Включает:
- Температуру и ощущаемую температуру
- Описание погоды на русском языке
- Скорость и направление ветра
- Влажность и атмосферное давление
- Эмодзи-иконки погодных условий

## 🔧 Требования

- EWW (Elkowars Wacky Widgets)
- curl
- jq
- OpenWeatherMap API ключ

## 📁 Структура файлов

```
~/.config/eww/
├── scripts/
│   ├── weather_gomel      # Основной скрипт погоды
│   └── weather_update     # Скрипт для cron обновления
├── eww.yuck              # Конфигурация виджета
├── eww.scss              # Стили виджета
└── README.md             # Этот файл
```

## 🚀 Установка

### 1. Получение API ключа OpenWeatherMap

1. Перейдите на https://home.openweathermap.org/api_keys
2. Зарегистрируйтесь или войдите в аккаунт
3. Создайте новый API ключ (бесплатный тариф)
4. Скопируйте API ключ

### 2. Настройка скрипта

Откройте файл `~/.config/eww/scripts/weather_gomel` и замените:
```bash
API_KEY="YOUR_API_KEY_HERE"  # Вставьте ваш API ключ
```
на:
```bash
API_KEY="ваш_api_ключ_здесь"
```

### 3. Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install curl jq
```

**Arch Linux:**
```bash
sudo pacman -S curl jq
```

**EWW installation:**
```bash
# Следуйте инструкциям: https://github.com/elkowar/eww
```

### 4. Первоначальное обновление данных

```bash
# Обновите данные погоды
~/.config/eww/scripts/weather_gomel update

# Проверьте получение данных
~/.config/eww/scripts/weather_gomel all
```

## 🎯 Использование

### Запуск виджетов

```bash
# Запустите EWW daemon
eww daemon

# Откройте основной виджет погоды
eww open weather

# Откройте компактный виджет для панели
eww open weather-bar

# Закройте виджеты
eww close weather
eww close weather-bar

# Перезапустите виджеты после изменения конфигурации
eww reload
```

### Автоматическое обновление

Добавьте в crontab для обновления каждые 30 минут:

```bash
crontab -e
```

Добавьте строку:
```cron
*/30 * * * * /home/username/.config/eww/scripts/weather_update
```

**Важно:** Замените `username` на ваше имя пользователя!

## 🎨 Настройка внешнего вида

В файле `eww.scss` вы можете настроить:

- **Цветовую схему:** измените значения `color` в классах
- **Размеры шрифтов:** измените значения `font-size`
- **Отступы и границы:** измените `padding`, `margin`, `border-radius`

### Основные классы стилей:

- `.weather-widget` - основной контейнер виджета
- `.weather-header` - заголовок с городом и временем
- `.weather-main` - основная информация (иконка, температура)
- `.weather-details` - детальная информация (ветер, влажность, давление)
- `.weather-compact` - компактная версия для панели

## 🔧 Доступные команды скрипта

Скрипт `weather_gomel` поддерживает следующие команды:

```bash
./scripts/weather_gomel update      # Обновить данные
./scripts/weather_gomel temp        # Температура
./scripts/weather_gomel feels_like  # Ощущаемая температура
./scripts/weather_gomel description # Описание погоды
./scripts/weather_gomel icon        # Иконка погоды
./scripts/weather_gomel wind_speed  # Скорость ветра
./scripts/weather_gomel wind_dir    # Направление ветра
./scripts/weather_gomel humidity    # Влажность
./scripts/weather_gomel pressure    # Давление
./scripts/weather_gomel city        # Город
./scripts/weather_gomel last_update # Время обновления
./scripts/weather_gomel all         # Вся информация
```

## 🎛️ Позиционирование виджетов

### Основной виджет (`weather`)
- Позиция: левый верхний угол (x: 10, y: 10)
- Размер: 400x280 пикселей

### Компактный виджет (`weather-bar`)
- Позиция: верхняя панель (x: 1200, y: 40)
- Размер: 200x30 пикселей

**Изменение позиции:** отредактируйте секцию `:geometry` в файле `eww.yuck`

## 🐛 Устранение проблем

### Не работает виджет

1. **Проверьте зависимости:**
   ```bash
   which curl jq
   ```

2. **Проверьте API ключ:**
   ```bash
   ~/.config/eww/scripts/weather_gomel update
   ```

3. **Проверьте синтаксис EWW:**
   ```bash
   eww validate
   ```

### Нет данных погоды

1. **Проверьте подключение к интернету**
2. **Убедитесь, что API ключ действителен**
3. **Проверьте, не превышен ли лимит запросов API**

### Не обновляются данные

1. **Проверьте настройки cron:**
   ```bash
   crontab -l
   ```

2. **Проверьте логи:**
   ```bash
   tail -f ~/.cache/eww/weather/update.log
   ```

## 📝 Примечания

- Данные кэшируются в `~/.cache/eww/weather/`
- API OpenWeatherMap бесплатный тариф: 1000 запросов в день
- Обновление настроено на каждые 30 минут (48 запросов в день)
- Русский язык для описаний погоды установлен по умолчанию

## 🎨 Темы и кастомизация

Вы можете легко адаптировать виджет под вашу тему оформления:

### Тёмная тема (текущая)
```scss
.weather-widget {
    background-color: rgba(33, 33, 37, 0.9);
    color: #D8DEE9;
}
```

### Светлая тема
```scss
.weather-widget {
    background-color: rgba(255, 255, 255, 0.9);
    color: #2E3440;
}
```

### Цветовая схема Catppuccin
```scss
.weather-widget {
    background-color: rgba(30, 30, 46, 0.9);
    border-color: rgba(137, 180, 250, 0.3);
}

.weather-temp {
    color: #89b4fa;
}
```

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте этот README
2. Проверьте официальную документацию EWW: https://elkowar.github.io/eww/
3. Создайте issue в репозитории (если применимо)

---

**Наслаждайтесь вашим новым виджетом погоды! 🌤️**